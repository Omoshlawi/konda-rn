import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { RouteMovementScreen } from "@/features/trip/screens";

const Route = () => {
  return <RouteMovementScreen />;
};

export default Route;

const styles = StyleSheet.create({});
